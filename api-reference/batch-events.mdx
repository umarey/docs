---
title: Batch Events
description: Ingest up to 100 events in a single request.
---

## POST /api/v1/events/batch

Submit an array of events for batch ingestion. All events are processed asynchronously. Maximum **100 events** per request.

### Request Body

<ParamField body="events" type="array" required>
  Array of event objects. Each object follows the same schema as the [single event endpoint](/api-reference/ingest-event).
</ParamField>

### Response

Returns `202 Accepted` with an array of queued event IDs:

```json
{
  "status": "queued",
  "events": [
    { "id": "9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d" },
    { "id": "a2c3d4e5-6f7a-8b9c-0d1e-2f3a4b5c6d7e" },
    { "id": "b3d4e5f6-7a8b-9c0d-1e2f-3a4b5c6d7e8f" }
  ]
}
```

### Examples

<CodeGroup>

```bash cURL
curl -X POST https://getimmutable.dev/api/v1/events/batch \
  -H "Authorization: Bearer imk_your_api_key_here" \
  -H "Content-Type: application/json" \
  -d '{
    "events": [
      {
        "actor_id": "user_2hG9kLm",
        "actor_name": "Sarah Chen",
        "action": "document.created",
        "resource_id": "doc_8nXpQr3",
        "resource_name": "Q4 Financial Report"
      },
      {
        "actor_id": "user_2hG9kLm",
        "actor_name": "Sarah Chen",
        "action": "document.shared",
        "resource_id": "doc_8nXpQr3",
        "resource_name": "Q4 Financial Report",
        "targets": [
          { "type": "team", "id": "team_5bR7cNx", "name": "Engineering" }
        ]
      },
      {
        "actor_id": "user_9pK3mNq",
        "actor_name": "James Rivera",
        "action": "comment.added",
        "resource_id": "doc_8nXpQr3",
        "resource_name": "Q4 Financial Report",
        "metadata": {
          "comment_id": "cmt_7xL2bRw",
          "body": "Looks good, approved."
        }
      }
    ]
  }'
```

```typescript JavaScript
const results = await immutable.sendBatch([
  immutable
    .event()
    .actor("user_2hG9kLm", { name: "Sarah Chen" })
    .action("document.created")
    .resource("doc_8nXpQr3", { name: "Q4 Financial Report" }),
  immutable
    .event()
    .actor("user_2hG9kLm", { name: "Sarah Chen" })
    .action("document.shared")
    .resource("doc_8nXpQr3", { name: "Q4 Financial Report" })
    .target("team", "team_5bR7cNx", { name: "Engineering" }),
  immutable
    .event()
    .actor("user_9pK3mNq", { name: "James Rivera" })
    .action("comment.added")
    .resource("doc_8nXpQr3", { name: "Q4 Financial Report" })
    .metadata({ comment_id: "cmt_7xL2bRw", body: "Looks good, approved." }),
]);
```

</CodeGroup>

<Note>
  Batch events are assigned `created_at` timestamps offset by 1 microsecond per event to preserve insertion order within the [hash chain](/guides/hash-chain).
</Note>

### Limits

- Maximum **100 events** per batch request.
- Each event counts toward your workspace's monthly event quota.
- Validation errors for individual events do not prevent other events in the batch from being processed.
